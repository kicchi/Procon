#include<bits/stdc++.h>
#define INF 2000000000
#define MOD 1000000007
#define EPS (1e-10)

using namespace std;


vector<int>  dx = {1,-1,0,0};
vector<int>  dy = {0,0,1,-1};





int main() {
	int N = 1000;
	vector<string> str(N);
	for (int i = 0; i < N; i++) {
		cin >> str[i];	
	}

	vector<vector<int>> button(9,vector<int>> 
	button[0] = {2,4};

	for (int i = 0; i < N; i++) {
		for (int j = 0; j < str[i].size(); j++) {
			if (str[j] == 'A') {
			/*
			}else if (str[j] == 'B') {
			}else if (str[j] == 'C') {
			}else if (str[j] == 'D') {
			}else if (str[j] == 'E') {
			}else if (str[j] == 'F') {
			}else if (str[j] == 'G') {
			}else if (str[j] == 'H') {
			}else if (str[j] == 'I') {
			*/
			}
		}
	}

	return 0;
}
